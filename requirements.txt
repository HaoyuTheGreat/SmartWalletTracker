# BigQuery — read/write raw_swaps / analyzed_swaps / wallet_classifications
google-cloud-bigquery==3.41.0

# Secret Manager — fetch HELIUS_API_KEY in cloud (local uses .env)
google-cloud-secret-manager==2.27.0

# HTTP client — Helius / Binance API
# 2.32.5 satisfies both google-cloud-bigquery (>=2.21, <3) and dune-client (~=2.32.5)
requests==2.32.5

# Reads .env file — local dev only
python-dotenv==0.21.0

# Dune Analytics — wallet candidate ingestion (Step 0 of pipeline)
dune-client==1.10.0
