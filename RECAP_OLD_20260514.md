# SWT 项目完整 RECAP

**生成日期**: 2026-05-14
**目的**: 把整个 pipeline 是什么、做了什么、怎么做的，每一步讲透。基于源码阅读，不是基于 README 摘要。

---

## 目录

1. [一句话概述](#一句话概述)
2. [整体架构](#整体架构)
3. [BigQuery Schema：9 张表](#bigquery-schema9-张表)
4. [Pipeline 5 个 Stage（数据流主线）](#pipeline-5-个-stage数据流主线)
   - [Stage 1: `ingest_wallets.py`](#stage-1-ingest_walletspy)
   - [Stage 2: `fetch_sol_prices.py`](#stage-2-fetch_sol_pricespy)
   - [Stage 3: `collect_traders_swaps.py`](#stage-3-collect_traders_swapspy)
   - [Stage 4: `analyze_wallets.py`](#stage-4-analyze_walletspy)
   - [Stage 5: `filter_traders.py`](#stage-5-filter_traderspy)
5. [Orchestrator: `main.py`](#orchestrator-mainpy)
6. [共享层 `lib/`](#共享层-lib)
7. [Infra 层](#infra-层)
8. [辅助脚本](#辅助脚本)
9. [部署 & 运行机制](#部署--运行机制)
10. [当前状态 & 最近改动](#当前状态--最近改动)
11. [已知 gaps / 未完成的部分](#已知-gaps--未完成的部分)

---

## 一句话概述

**SWT** 每天 9am UTC 在 GCP Cloud Run 上跑一次完整 pipeline：从 Dune 拿候选钱包 → 通过 Helius 拉这些钱包在 Solana 上的 swap 交易 → 解析成统一格式 → 按行为/盈亏分类。所有数据落 BigQuery。最终输出是 `wallet_classifications` 表里被标 `smart_candidate` 的钱包列表。

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│  External APIs (3 个)                                        │
│  ┌──────┐  ┌────────┐  ┌─────────┐                          │
│  │ Dune │  │ Helius │  │ Binance │                          │
│  └──┬───┘  └───┬────┘  └────┬────┘                          │
└─────┼──────────┼────────────┼───────────────────────────────┘
      │          │            │
      ▼          ▼            ▼
┌─────────────────────────────────────────────────────────────┐
│  GCP Cloud Run Job (daily 9am UTC trigger via Scheduler)     │
│  Container: python:3.11-slim + pipeline code                 │
│  Entry: python main.py → runs 5 stages sequentially          │
└─────────────────────┬───────────────────────────────────────┘
                      │
       ┌──────────────┼────────────────┐
       │              │                │
       │ secrets      │ writes         │
       ▼              ▼                ▼
┌─────────────┐  ┌──────────────┐  ┌─────────────┐
│ Secret      │  │  BigQuery    │  │ Cloud       │
│ Manager     │  │  (9 tables)  │  │ Logging     │
│ (3 keys)    │  │              │  │             │
└─────────────┘  └──────────────┘  └─────────────┘
```

**两个 service account**（least privilege）：
- `smartwallets-runner@...` — Job 跑的时候用，有 BigQuery dataEditor + Secret Manager 读权限
- `smartwallets-scheduler@...` — Scheduler 触发 Job 用，只有 run.invoker 权限（不能读数据）

---

## BigQuery Schema：9 张表

数据集名 `smart-wallets-tracker.whale_tracker`。9 张表分成 3 类：

### A. 数据主线（5 张）

| 表 | 角色 | 主键（逻辑）| Partition + Cluster |
|---|---|---|---|
| `wallet_candidates` | Dune buffer | `(address, source)` | PARTITION BY DATE(discovered_at), CLUSTER BY address |
| `wallets` | 业务追踪集合 | `address` (PK), `wallet_id = address[:8]` | — |
| `raw_swaps` | 原始 Helius JSON | `(wallet_id, signature)` | PARTITION BY DATE(tx_time), CLUSTER BY wallet_id |
| `analyzed_swaps` | 解析后的统一 schema | `(wallet_id, signature)` | PARTITION BY DATE(swap_time), CLUSTER BY wallet_id |
| `wallet_classifications` | 分类快照（append-only）| `(wallet_id, classified_at)` | PARTITION BY DATE(classified_at), CLUSTER BY wallet_id |

### B. 旁路 / 元数据（3 张）

| 表 | 用途 |
|---|---|
| `wallet_sources` | provenance —"哪个源在什么时候看到过这个钱包，看了几次" |
| `ingestion_runs` | 审计日志 — 每次 Stage 1 run 写一行（success/failed + 计数 + traceback）|
| `sol_prices` | side dimension — 每天一行 SOL/USD 收盘价 |

### C. 配置 / 字典（1 张）

| 表 | 用途 |
|---|---|
| `exchange_wallets` | CEX 黑名单（**当前为空，没人工填充**）|

**Schema 定义文件**：
- [infra/bigquery_schema.sql](infra/bigquery_schema.sql) — 最初的 5 张表
- [infra/migrations/002_add_ingestion_tables.sql](infra/migrations/002_add_ingestion_tables.sql) — 后加的 4 张 ingestion 相关表

---

## Pipeline 5 个 Stage（数据流主线）

`main.py` 顺序执行这 5 个 stage。Stage 1 是 **fail-soft**（失败不影响后面），Stages 2-5 是 **fail-hard**（任何一个失败整个 Job exit 1，Scheduler 把这次 run 标 failed）。

```
Stage 1 ingest        Dune → wallet_candidates + wallets
Stage 2 sol_prices    Binance → sol_prices
Stage 3 collect       wallets + Helius → raw_swaps
Stage 4 analyze       raw_swaps → analyzed_swaps  (+ Helius DAS for token symbols)
Stage 5 filter        raw_swaps + analyzed_swaps → wallet_classifications
```

---

### Stage 1: `ingest_wallets.py` — Daily wallet candidate ingestion

**做什么**：从外部 source ingest wallet candidates + 决定哪些进入业务追踪。

**Pipeline 失败语义**：**fail-soft** — Dune 挂掉不影响 Stage 2-5。`main()` 用单独的 try/except 包它，失败只 print warning 继续跑。

**写入的 4 张 BQ tables**（一次 run）:
- `wallet_candidates` — 候选 buffer + 处置 status
- `wallet_sources` — provenance (which source saw which wallet, seen_count)
- `wallets` — top N 新 wallets (业务驱动表)
- `ingestion_runs` — audit log (一行 per run per source, success or failure)

---

#### [1] Dune → wallet_candidates (MERGE upsert)

通过 `DuneAdapter` instance 拉 ~5000 candidates，MERGE 进 `wallet_candidates`：
- 新 (address, source) pair → INSERT with `status='pending'`
- 已存在 pair → UPDATE `raw_metrics`，**status 保持** (不动 'promoted' / 'filtered_out')

**Why MERGE (not INSERT)?**

Dune 每天返回的 5000 钱包基本重复 (top 钱包排名稳定)。用 plain INSERT 会让表线性增长 — 一年 ~180万行，其中 ~99% 是 duplicates。用 MERGE on `(address, source)`：已存在的 trigger UPDATE 而不是新增 row。

**实际效果**：项目跑几周后，`wallet_candidates` 大约 5018 行，接近一天 Dune 量，不是 weeks × 5000。

**Why MERGE 不动 status?**

`status` 是**业务状态** ('promoted' / 'filtered_out' 来自之前 run 的决定)。如果每天 MERGE 把它重置回 'pending'，之前的 promote 状态全丢，重大 bug。

**Adapter setup vs work**：

`DuneAdapter()` instantiation **不调 Dune API** — 只读 `DUNE_API_KEY` + 创建 `DuneClient` object。真正的 HTTPS call 发生在 `.fetch_candidates()` 被调用时。

**Return value 用 tuple unpacking**：

```python
new_count, updated_count = bq.upsert_wallet_candidates(candidates, source, ...)
```

`new_count` 写进 `summary["candidates_new"]` 给 audit log。

---

#### [2] 同步维护 wallet_sources (provenance, parallel write)

紧接着 wallet_candidates MERGE，同一批 addresses 也 MERGE 进 `wallet_sources`：

```python
addresses = [c.address for c in candidates]
bq.upsert_wallet_sources(addresses, source)
```

**两张表的角色差别**：
- `wallet_candidates`：完整 metadata + 处置 status (重表)
- `wallet_sources`：轻量 provenance — `(address, source)`, `first_seen_at`, `last_seen_at`, `seen_count`

`seen_count` 每次 MERGE 都 +1 — 累加器。设计目的：
- 未来加 BirdEye / Arkham 时，同一钱包被多源看到 → 信号强度排序
- "持续被同一源看到 N 次的钱包优先级更高" 这种逻辑

**Status**：当前下游 stage 未消费这些信号 — 为未来准备的元数据层。

---

#### [3] 内存里把 5000 candidates 分类成 3 个 bucket

Pre-allocated containers with type hints：

```python
to_insert_all: list[str] = []       # 新合格的, 保留 Dune 的 volume DESC 顺序
already_tracked: list[str] = []     # 已在 wallets 表的
to_filter: dict[str, str] = {}      # CEX-flagged, key=address, value=reason
```

Single-pass classification (一次 for-loop)：

```python
for c in candidates:
    if c.address in cex_set:
        to_filter[c.address] = "known_cex"           # CEX 不是 smart money
    elif c.address in existing_wallets:
        already_tracked.append(c.address)             # 已 promote 过, 跳过 INSERT
    else:
        to_insert_all.append(c.address)               # 候选池, 等下取 top N
```

**Container 选择的 nuance**：
- `to_filter` 是 `dict` (而不是 list)，因为要同时存 address + 过滤原因
- 其他两个是 list (只需要 address)

**Order preservation**：

`to_insert_all` 用 `append()`，保留 Dune 的 volume DESC 顺序。下一步 `[:20]` 取的是**真正高 volume 的 20 个**，不是随机。

---

#### [4] Top N promotion + 3 个 BQ writes

```python
to_insert_today = to_insert_all[:DAILY_PROMOTION_LIMIT]   # = [:20]
deferred_count = len(to_insert_all) - len(to_insert_today)

# Write 1: INSERT 新行进 wallets 表
if to_insert_today:
    bq.insert_wallets_from_candidates(to_insert_today, source)

# Write 2: UPDATE wallet_candidates.status='promoted' (今天 promote 的 + already_tracked)
promoted_now = to_insert_today + already_tracked
if promoted_now:
    bq.mark_candidates_promoted(promoted_now, source)

# Write 3: UPDATE wallet_candidates.status='filtered_out' (CEX)
if to_filter:
    bq.mark_candidates_filtered(to_filter, source)
```

**Why top 20?**

Rate limit 保护下游 Helius API quota。每个 promoted wallet 在 Stage 3 最多消耗 2000 swap fetches。20/day cap 让 monthly cost 接近 free tier 上限 (~1.2M Helius credits/month)。

**Why also mark already_tracked as 'promoted'?**

already_tracked 钱包**之前已经 promote 过**，但它们在 wallet_candidates 表里的 status 可能还是 'pending' (例如来自更早 migration)。这一步**修正 status**，确保 wallet_candidates 与 wallets 表 consistent。

**`mark_candidates_filtered` 内部优化**：

Group by reason (用 `defaultdict(list)`) → 一个 reason 一次 UPDATE，而不是 per-address N 次 UPDATE — 减少 BQ round-trip。

---

#### [5] Deferred candidates 保持 'pending'

没被 top N 取到的 candidates 保持 `status='pending'` (MERGE 不动它们，也没 mark_* 函数动它们)。

明天 Dune 再返回这些 candidates 时，它们又会：
- MERGE 命中已有行 → UPDATE raw_metrics，status 保持 'pending'
- 还不在 wallets 表 → 进 `to_insert_all`
- 又有机会被取 top 20

**Drip-feed pattern**：500 个 deferred ≈ 25 天自然消化完。

**Status lifecycle 是单向的 (monotonic)**：

```
INSERT (新 (address, source))
   ↓
'pending'
   │
   ├──→ 'promoted'      (一次性, 之后 MERGE 不改它)
   ├──→ 'filtered_out'  (一次性, 之后 MERGE 不改它)
   └──→ 还是 'pending'  (没人动, 等明天)
```

---

#### [6] Summary dict + audit log via try/finally

整个 `run_source(adapter)` 用 **accumulator pattern** 收集 metrics：

```python
def run_source(adapter):
    run_id = str(uuid.uuid4())                # UUID4, 本地生成, distributed-safe
    source = adapter.source_name
    started_at = _utcnow_iso()

    summary = {
        "source": source,
        "run_id": run_id,
        "candidates_fetched": 0,
        "candidates_new": 0,
        "promoted_to_wallets": 0,
        "status": "failed",                    # Pessimistic default
        "error_message": None,
    }

    try:
        # 每步成功后 update summary[xxx]
        candidates = adapter.fetch_candidates()
        summary["candidates_fetched"] = len(candidates)

        new_count, _ = bq.upsert_wallet_candidates(...)
        summary["candidates_new"] = new_count

        # ... 后续步骤 ...

        summary["promoted_to_wallets"] = len(to_insert_today)
        summary["status"] = "success"          # 只有跑到最后才改 success
    except Exception as e:
        summary["error_message"] = f"{type(e).__name__}: {e}\n{traceback.format_exc()[:1500]}"
    finally:
        bq.log_ingestion_run(...)              # 不管成功失败都写一行 audit log

    return summary
```

**三个 senior 设计**：

1. **Pessimistic default** (`status="failed"`)：如果中途 crash 来不及 set status，audit log 仍正确显示 failed。**避免 false-positive success records**。
2. **Incremental update**：每步成功后立刻 update summary，所以即使后续 crash，summary 反映 partial progress (diagnostic value)。
3. **try/finally guarantees**：finally 块**始终运行**，保证 audit log 一定写进 `ingestion_runs`。

**Diagnostic value — 字段位置告诉你失败在哪一步**：

| summary 字段值 | 可推断的失败位置 |
|---|---|
| 全 0, status="failed" | 在 fetch_candidates 之前就挂 (e.g., DUNE_API_KEY missing) |
| candidates_fetched > 0, 其他 0 | fetch 成功, 但 upsert_wallet_candidates 之前挂 |
| candidates_new > 0, promoted_to_wallets = 0 | MERGE 成功, 但 promotion 那段挂 |
| 全非 0, status="failed" | 主流程成功, 后处理 (mark_*) 挂 |
| 全非 0, status="success" | 完全成功 |

比翻 log file 调 bug 快 5-10×。

---

#### [7] Adapter pattern enables source pluggability

`SourceAdapter` ABC (in `lib/adapters/base.py`) 定义所有 adapter 必须实现的契约：

```python
class SourceAdapter(ABC):
    @property
    @abstractmethod
    def source_name(self) -> str: ...

    @abstractmethod
    def fetch_candidates(self) -> list[Candidate]: ...

    @property
    def source_query_id(self) -> str | None:
        return None
```

`DuneAdapter` 在 `lib/adapters/dune_adapter.py` 实现这个契约。

**加新 source 的全部成本**：

1. 写 `lib/adapters/birdeye_adapter.py` 实现 `SourceAdapter`
2. 在 `ingest_wallets.get_sources()` 加一行 `BirdEyeAdapter()`

`main()` 一行不改, `run_source(adapter)` 一行不改。这是 **Open/Closed Principle** (open for extension, closed for modification)。

**两层 wrapper 好处**：
- **Isolation**：Dune SDK 改动只影响 `dune_adapter.py`
- **Polymorphism**：pipeline 用 `SourceAdapter` 统一接口处理所有 sources，不需要 if/else 检查 adapter 类型

---

#### [8] Dune frequency nuance

SWT 每天调 Dune (Cloud Scheduler 9am UTC) 用 `get_latest_result()`：
- **0 credits** (读 Dune 端 cached 结果)
- **不触发 fresh execution** (Dune free plan API 不支持 `/execute`)

Dune query 真正重新执行需要**手动在 Dune UI 触发** (推荐 weekly)。

所以 candidate pool 的真正刷新由你手动控制。每天 ingest 都拿到**同一份 cached 数据**，这就是为什么 `wallet_candidates` 表 "按你刷 Dune 的频率" 阶梯式增长 — 不是 daily 5000 新增。

---

#### Stage 1 写入 BQ tables 全景

| 表 | 操作 | 行数变化 (per run) | 写入函数 |
|---|---|---|---|
| `wallet_candidates` | MERGE (UPSERT) | +0 到 +few (大部分 UPDATE) | `bq.upsert_wallet_candidates` |
| `wallet_candidates` (status) | UPDATE | 至多 20 个改 'promoted' + N 个改 'filtered_out' | `bq.mark_candidates_promoted` / `bq.mark_candidates_filtered` |
| `wallet_sources` | MERGE | +0 到 +few (大部分 UPDATE, seen_count +1) | `bq.upsert_wallet_sources` |
| `wallets` | INSERT | +0 到 +20 (新 promote 的) | `bq.insert_wallets_from_candidates` |
| `ingestion_runs` | INSERT | +1 (per source per run, success or failure) | `bq.log_ingestion_run` |

---

#### TL;DR (面试一句话讲法)

> Stage 1 daily 从 Dune 拉 ~5000 wallet candidates，**MERGE** 进 `wallet_candidates` 避免重复暴增，内存里分类成 3 个 bucket (CEX-blacklisted / already-tracked / new-qualified)，从 new 候选取 top 20 promote 进 `wallets` 表 (rate-limited to bound downstream Helius API cost)。用 **summary dict + try/finally** 保证 audit log 在 `ingestion_runs` 一定被写入，即使中途 crash。整个 stage 是 **fail-soft** — Dune 挂不拖死 pipeline。**Adapter pattern** (SourceAdapter ABC) 让加新 source 只需新文件 + 一行注册。

---

### Stage 2: `fetch_sol_prices.py`

**做什么**：增量拉 SOL/USD 历史价格。

#### 详细步骤

1. **`_latest_price_date_ms()`** —— 调 `bq.fetch_sol_price_map()` 拿现有 `{date: price}` map，取最大 date 转毫秒。第一次跑（表空）返回 0。
2. **循环拉 Binance** ([fetch_sol_prices.py:44-68](fetch_sol_prices.py))：
   - URL: `https://api.binance.us/api/v3/klines?symbol=SOLUSD&interval=1d&startTime=<ms>&limit=1000`
   - 每次返回最多 1000 根 K 线，取每根的 `open_time` 和 `close` 价格
   - 翻页：`start_time = klines[-1][0] + 1_day_ms`
   - 终止条件：返回 < 1000 行，说明没更多了
   - 翻页之间 `time.sleep(0.2)`
3. **`bq.upsert_sol_prices(prices)`** ([lib/bq.py:293-334](lib/bq.py))：
   - Staging table + MERGE on `price_date`
   - 已存在的日期：UPDATE `price_usd`
   - 新日期：INSERT

#### 关键设计点

- **零 API 成本**：Binance 这个 endpoint 不需要 API key
- **增量**：第一次拉 1400+ 天历史，之后每天只补 1 天
- **Idempotent**：用 MERGE，重复跑安全

#### 输出
| 表 | 写的内容 |
|---|---|
| `sol_prices` | 每天一行（增量 MERGE）|

---

### Stage 3: `collect_traders_swaps.py`

**做什么**：从 Helius 拉每个钱包的最新 SWAP 交易，存原始 JSON。

#### 详细步骤

1. **`bq.fetch_wallets_needing_collection(max_age_hours=24)`** —— 拉所有"上次刷新 > 24 小时 或 从未刷新"的钱包，且 `collection_status != 'failed'`
2. **For each wallet**：
   - **3.1** `bq.existing_signatures_for_wallet(wallet_id)` —— 1 个 BQ query 拿这个钱包已有的所有 signature（用于增量去重）
     - ⚠️ **这是 N+1 query 模式** —— 1000 钱包 = 1000 个 BQ query
   - **3.2** `fetch_new_swaps(address, existing_sigs)`：
     - Helius endpoint: `https://api-mainnet.helius-rpc.com/v0/addresses/{addr}/transactions/?api-key=...&type=SWAP&limit=100`
     - **Helius 默认 newest-first 返回**
     - 翻页：`before = page[-1].signature`
     - **核心增量逻辑**：if `sig in existing_sigs: stop_early = True; break` —— 因为 newest-first，命中已知 sig = 后面更老的都有了
     - Hard cap：`MAX_TX_PER_WALLET = 2000`（防极端钱包炸 API quota）
     - 每页之间 `time.sleep(0.2)`
   - **3.3** 写入逻辑（三分支）：
     - 有新 txs → `bq.insert_raw_swaps(wallet_id, new_txs)` → `update_wallet_status(wallet_id, "ok")`
     - 没新 txs 但有老数据 → 只 `update_wallet_status(wallet_id, "ok")`（已是最新）
     - 没新 txs 且没老数据 → `update_wallet_status(wallet_id, "failed")`（这个钱包永远跳过）
   - **3.4** 钱包之间 `time.sleep(0.2)`

#### 关键设计点

- **Sig hit-stop 增量**：依赖 Helius 的 newest-first 排序保证，命中已知 sig 立刻 break。大部分钱包每次只拉 1-2 页就停
- **raw_json 字段保留**：`bq.insert_raw_swaps` ([lib/bq.py:122-143](lib/bq.py)) 把整个 Helius 原始 JSON 用 `json.dumps()` 序列化存进 BQ STRING 字段 —— 这样将来 parser 升级了可以从 raw 重算，不用再调 Helius
- **per-wallet failed flag**：彻底没数据的钱包永久跳过，避免每天浪费 API quota 确认"还是没动"
- **Hard cap 2000**：新钱包首次 backfill 时用到。20 页 × 100 = 2000

#### 性能问题（你目前的 pain point）

- 串行跑 1000 钱包 ≈ 10-20 分钟（受 Helius 速率限制 + per-wallet BQ query）
- N+1 BQ query 模式没解决
- Helius credit 消耗大头（约占总用量 80%+）

#### 输出
| 表 | 写的内容 |
|---|---|
| `raw_swaps` | 新交易 INSERT（按钱包分批 load job）|
| `wallets` | `collection_status` + `last_collected_at` UPDATE |

---

### Stage 4: `analyze_wallets.py`

**做什么**：把 `raw_swaps` 的原始 JSON 解析成统一格式（不管是哪个 DEX），存进 `analyzed_swaps`。

#### 详细步骤

1. **Phase 1 — 只问 BQ 要需要解析的**：
   - **4.1** `bq.fetch_unanalyzed_raw_swaps(PARSER_VERSION=7)` —— **SQL anti-join**：
     ```sql
     SELECT r.wallet_id, r.signature, r.raw_json
     FROM raw_swaps r
     LEFT JOIN analyzed_swaps a
       ON r.wallet_id = a.wallet_id
      AND r.signature = a.signature
      AND a.parser_version = @v
     WHERE a.signature IS NULL
     ```
   - 这是著名的 **189s → 2s 优化**（[lib/bq.py:180-207](lib/bq.py)）
   - 如果一切已是最新，这个 query 返回空 → 整个 stage 1 秒内退出（"ask only what you need" gate）
2. **Phase 2 — 加载辅助数据**：
   - `bq.fetch_all_wallets()` —— `wallet_id → address` 映射（解析时需要 address 来认 from/to）
   - `bq.fetch_sol_price_map()` —— `date → price_usd`
   - `load_initial_token_cache()` —— 先试 `bq.fetch_token_symbol_cache()`（从已 analyzed 的 ARRAY<STRUCT> 里 UNNEST 出 mint→symbol），空的话 fallback 到 `data/token_names.json`
3. **Phase 3 — 解析**：
   - **`parse_swap(tx, wallet_address)`** 派发逻辑：
     - `source == "JUPITER"` → 用 `parse_jupiter()`：读 `events.swap.tokenInputs/Outputs/nativeInput/nativeOutput`，结构化提取 SOL 量 + token 量
     - 若 Jupiter 解析结果全空 → fallback 到 `parse_by_token_transfers()`
     - 其他 source 直接走 `parse_by_token_transfers()`：扫 `tokenTransfers[]`，按 `fromUserAccount == address` 算 spent，`toUserAccount == address` 算 received
   - **`collect_mints` + `resolve_token_symbols`**：
     - 收集这批 parsed 里所有新 mint
     - 调 Helius DAS `getAsset` 拿 symbol（3 次重试 + 2s backoff，最终失败用 `mint[:8]` 兜底）
   - **`build_row()`**：
     - 算 `sol_price_usd = sol_prices.get(date_str, 150)` —— 缺数据 fallback 用 150
     - 把 `{mint: amount}` 转成 `[{mint, symbol, amount}, ...]`（这是 BQ ARRAY<STRUCT> 的形状）
     - 加 `parser_version = 7`
4. **Phase 4 — 单次 batch insert**：
   - 所有 wallet 的所有 parsed rows 攒到一个 list
   - 一次性 `bq.insert_analyzed_swaps(all_rows)` —— **61 个 load job → 1 个**

#### 关键设计点

- **Predicate pushdown**：anti-join 让 BQ 算"还没解析的差集"，Python 只收要做的活 —— 189s → 2s
- **PARSER_VERSION = 7**：bump 一下这个常量就能强制全表 re-parse（anti-join 自动让所有 row 看起来都没 v8 处理过）
- **Token symbol cache 从 analyzed_swaps UNNEST**：之前解析过的 (mint, symbol) 对自动复用 —— 重跑几乎不调 Helius DAS
- **多 DEX coverage（7+ DEX）**：Jupiter parser 用 Jupiter 自己的 events.swap 结构；其他 DEX 用通用 token-transfer fallback。README 声称 98.5% coverage

#### 输出
| 表 | 写的内容 |
|---|---|
| `analyzed_swaps` | 解析后的统一 schema（单次 batch insert）|

---

### Stage 5: `filter_traders.py`

**做什么**：把 swap 历史聚合成钱包行为标签 + PnL 数字，append 到 `wallet_classifications`。

#### 详细步骤（最近改成增量了）

1. **`bq.fetch_wallets_needing_classification()`** —— **新增的增量 anti-join**：
   ```sql
   WITH last_classified AS (
     SELECT wallet_id, MAX(classified_at) AS last_at
     FROM wallet_classifications
     GROUP BY wallet_id
   )
   SELECT DISTINCT a.wallet_id
   FROM analyzed_swaps a
   LEFT JOIN last_classified lc USING(wallet_id)
   WHERE lc.last_at IS NULL OR a.analyzed_at > lc.last_at
   ```
   返回"从来没分类过 + 有新 analyzed 数据"的钱包列表（[lib/bq.py:269-287](lib/bq.py)）
2. **如果空集合 → 直接 return**（"all up to date"）
3. **`bq.fetch_all_wallets()`** → filter 出只在 pending_set 里的（拿到 `address` 字段）
4. **`bq.fetch_raw_swaps_all_wallets(wallet_ids=pending_ids)`** —— 拉这些钱包的全部 raw_swaps（`WHERE wallet_id IN UNNEST(@ids)`）
5. **`bq.fetch_analyzed_swaps_all_wallets(wallet_ids=pending_ids)`** —— 同上拉 analyzed
6. **For each wallet, `classify_wallet(wallet_id, address, raw_swaps, analyzed_swaps)`**：

   **行为标签**（用 `raw_swaps` 算，因为需要 `tokenTransfers` / `events.swap` 这些 raw 字段）：
   - `is_proxy_transaction(tx)`：钱包不在 tokenTransfers 且无 events.swap → 视为 proxy 调用；`proxy_pct > 50%` → 标 `proxy_bot`
   - `calc_daily_frequency(swaps)`：`(last - first) / 86400` 天，频率 = 交易数 / 天数；`> 20/day` → `high_frequency`
   - `total_swaps < 20 OR active_days < 7` → `insufficient_data`
   - `is_market_maker()`：高频 + 主导 token 的 buy/sell 比例对称（min/max > 0.5）+ 集中度（top token > 40%）→ `market_maker`

   **PnL（用 `analyzed_swaps` 算）**：
   - `aggregate_by_token()`：
     - USDC/USDT 折算成 "virtual SOL"（除以当日 SOL price）
     - WSOL 排除（已经被 native SOL 计过）
     - 按 mint 聚合 `bought / sold / sol_in / sol_out`
   - `calc_performance()`：
     - Position 视为 closed 当 `sold >= bought * 0.95`
     - `pnl_sol = sol_out - sol_in`
     - `sold > bought * 1.1` → 算 inflated（可能是数据被 2000 cap 砍了）→ 标 `data_clipped`
     - `win_rate = wins / closed * 100`

   **最终判定**：
   - 排除标签集 = `{proxy_bot, high_frequency, insufficient_data, market_maker}`
   - 没排除标签 AND `closed_positions >= 5` AND `win_rate > 50` AND `total_pnl_sol > 0` → 标 `smart_candidate`
7. **`bq.insert_classifications(rows)`** —— 一次性 append 进 `wallet_classifications`（带当前 `classified_at` 时间戳）

#### 关键设计点

- **Append-only snapshot**：每次 run 不覆盖老数据，每个钱包一行新 snapshot。可以查"这个钱包上周到这周分类变了没"
- **Raw 用于行为，analyzed 用于 PnL**：故意分离，因为 analyzed 已经丢掉了 `tokenTransfers` 等结构信息
- **6 个标签**：`smart_candidate` / `market_maker` / `proxy_bot` / `high_frequency` / `insufficient_data` / `data_clipped`
- **增量（5/13 刚做）**：之前每次跑都重新加载所有钱包的全部历史（导致 5/13 撞 3600s timeout）。现在只 fetch + classify"上次分类后有新数据"的钱包

#### 输出
| 表 | 写的内容 |
|---|---|
| `wallet_classifications` | 每个需要重 classify 的钱包一行 |

---

## Orchestrator: `main.py`

[main.py](main.py) 是 Cloud Run Job container 的入口（Dockerfile CMD 是 `python main.py`）。

**核心逻辑**：
- `run_step(name, func)` —— 包一层：打 banner、计时、捕异常、re-raise
- Stage 1 单独 try/except —— 失败只 print warning，不影响后面（**fail-soft**）
- Stages 2-5 共用一个 try/except —— 任一失败 `sys.exit(1)`（**fail-hard**）
- 最后打总耗时 + 每个 stage 用了多少秒

**为什么 Stage 1 fail-soft 而 Stages 2-5 fail-hard**：
- Dune 偶尔挂 ≠ 整个 pipeline 应该挂；已有的 wallets 还能处理
- 但如果 Helius / BQ 挂了，下游全 corrupt，必须 stop

---

## 共享层 `lib/`

### `lib/bq.py` — BigQuery DAL（582 行）

**唯一一个接触 `bigquery.Client` 的地方**。pipeline scripts 全部通过它读写。

按表分块：
| 块 | 主要函数 |
|---|---|
| wallets | `fetch_all_wallets` / `fetch_wallets_needing_collection` / `update_wallet_status` |
| raw_swaps | `existing_signatures_for_wallet` / `insert_raw_swaps` / `fetch_raw_swaps_all_wallets` / `fetch_unanalyzed_raw_swaps` |
| analyzed_swaps | `fetch_token_symbol_cache` / `insert_analyzed_swaps` / `fetch_analyzed_swaps_all_wallets` / `fetch_wallets_needing_classification` (NEW) |
| sol_prices | `upsert_sol_prices` (MERGE) / `fetch_sol_price_map` |
| wallet_classifications | `insert_classifications` |
| 候选 ingestion 系列 | `upsert_wallet_candidates` (MERGE) / `upsert_wallet_sources` (MERGE) / `fetch_exchange_wallet_addresses` / `fetch_existing_wallet_addresses` / `insert_wallets_from_candidates` / `mark_candidates_promoted` / `mark_candidates_filtered` / `log_ingestion_run` |

**几个 helper**：
- `client()` —— lazy init，进程生命周期内只创建一次
- `_table(name)` —— 拼接完整表名 `project.dataset.name`
- `_load_rows(table, rows, write_truncate=False)` —— 通用 batch load via NEWLINE_DELIMITED_JSON
- `_utcnow_iso()` —— UTC ISO 时间戳

**MERGE pattern**（用在 sol_prices、wallet_candidates、wallet_sources）：
1. 临时 staging table 名字带 UNIX 时间戳避免冲突
2. `load_table_from_json(rows, staging)` 把数据装进 staging
3. `MERGE target USING staging ON key WHEN MATCHED ... WHEN NOT MATCHED INSERT ...`
4. `delete_table(staging, not_found_ok=True)` 清理

### `lib/secrets.py` — 三层 secret 解析

```python
get_secret("HELIUS_API_KEY")
```

解析顺序：
1. **`os.getenv(name)`** —— 进程环境变量
2. **`.env` 文件** —— `load_dotenv()` 已在 import 时调过，把 .env 的值灌进 os.environ
3. **GCP Secret Manager** —— 调 `secretmanager.SecretManagerServiceClient`，路径 `projects/{project}/secrets/{name}/versions/latest`

`@lru_cache(maxsize=32)` 装饰过，进程内每个 key 只解析一次。

### `lib/adapters/` — Adapter 模式

- [base.py](lib/adapters/base.py) — `SourceAdapter` ABC + `Candidate` dataclass
- [dune_adapter.py](lib/adapters/dune_adapter.py) — Dune 的具体实现，调 `DuneClient.get_latest_result(query_id=7335552)`
- [__init__.py](lib/adapters/__init__.py) — re-export

#### `Candidate` dataclass — 跨 source 统一的数据 shape

`Candidate` 是个 `@dataclass`，把不同数据源 (Dune / 未来 BirdEye / Arkham) 返回的钱包数据**统一打包**成 Python object：

```python
@dataclass
class Candidate:
    address: str                                          # 钱包地址 (string)
    chain: str                                            # 例如 "solana"
    raw_metrics: dict[str, Any] = field(default_factory=dict)   # 源的原始指标 (volume, trade_count, ...)
```

任何 adapter 的 `fetch_candidates()` 都必须返回 `list[Candidate]`。这是跨 source 的统一**接口数据形状**。

#### "candidates 里每个 element 是 Candidate instance" — 在 3 个地方定义

1. **Contract (declaration)** — `base.py` 的 `SourceAdapter.fetch_candidates()` 签名：
   ```python
   @abstractmethod
   def fetch_candidates(self) -> list[Candidate]: ...
   ```
   Type hint 规定所有 adapter 的返回类型。

2. **Implementation signature** — `dune_adapter.py` 的 `DuneAdapter.fetch_candidates()`：
   ```python
   def fetch_candidates(self) -> list[Candidate]:
   ```
   重申返回类型，匹配契约。

3. **Runtime instantiation** — `DuneAdapter.fetch_candidates()` 函数 body 里：
   ```python
   candidates: list[Candidate] = []
   for row in rows:
       candidates.append(Candidate(                # ← 每行创建一个新 Candidate instance
           address=row.get("wallet_address"),
           chain="solana",
           raw_metrics={
               "trade_count":        row.get("trade_count"),
               "total_volume_usd":   row.get("total_volume_usd"),
               ...
           },
       ))
   return candidates
   ```
   **这一行才是真正创建 Candidate object 的地方**。前两层是 declaration / type hint，这里是 execution。

#### Type hint vs runtime: 重要的区别

- **Type hint** (`list[Candidate]`) — Python 不在 runtime 强制检查。只是给 reader / IDE / mypy 的注释。
- **Runtime 实际类型** — 由 `Candidate(...)` 调用决定。`candidates.append(...)` 这一行才**真正决定** list 里装什么。

如果 type hint 写 `list[Candidate]` 但 body 里 `append(some_string)`，**runtime 不会报错** — type hint 只是 documentation + tool hint。Senior 实践: type hint 和 implementation **保持一致**, 不依赖 magic enforcement。

#### `candidates` list 是 method return value, 不是 instance attribute

`candidates` list 是在 `DuneAdapter.fetch_candidates()` 函数**内部创建**的 **local variable**。函数通过 `return candidates` 把这个 list 作为返回值传给 caller。Caller (`ingest_wallets.run_source`) 写 `candidates = adapter.fetch_candidates()`，用**自己的 local variable** 接收返回值。

```python
# dune_adapter.py
def fetch_candidates(self) -> list[Candidate]:
    candidates: list[Candidate] = []     # ← local variable 在这里创建
    for row in rows:
        candidates.append(Candidate(...))
    return candidates                     # ← 函数把 list 作为返回值送出去

# ingest_wallets.py
def run_source(adapter):
    candidates = adapter.fetch_candidates()   # ← caller 用自己的 local variable 接收
    # ... use candidates ...
```

**两个函数里都叫 `candidates`，但是两个不同的 variable name，指向同一个 list object** (Python 传递 reference，不是 copy)。

| | `candidates` in `dune_adapter.py` | `candidates` in `ingest_wallets.py` |
|---|---|---|
| Scope | `DuneAdapter.fetch_candidates()` 函数内 | `run_source()` 函数内 |
| Lifetime | 函数执行期间存在；函数返回后这个 name 消失 | 函数执行期间存在；函数返回后这个 name 消失 |
| 指向的 object | 同一个 list object | 同一个 list object |

**重要：`candidates` 不是 `DuneAdapter` 的 instance attribute**。`DuneAdapter` 只有 `self._client` 和 `self._query_id` 两个 instance attributes，**没有 `self.candidates`**。

```python
adapter = DuneAdapter()
adapter.candidates           # ❌ AttributeError — 没这个属性
adapter.fetch_candidates()   # ✓ 调 method 才拿到 list (作为返回值)
```

要拿这个 list，必须**调 method + 接收返回值**，不能"借用 instance 状态"。

#### 数据流

```
Dune API
   │ get_latest_result() 返回 dict 列表
   ▼
response.result.rows   (Dune 原始格式)
   │ for row in rows:
   ▼
Candidate(address=..., chain="solana", raw_metrics={...})   ← Instantiation 发生在这一行
   │ candidates.append(...)
   ▼
candidates: list[Candidate]   ← 5000 个 Candidate instances
   │ return
   ▼
ingest_wallets.py 接收到这个 list, 之后用 c.address / c.chain / c.raw_metrics 访问属性
```

**加新数据源的流程**：
1. 新建 `lib/adapters/birdeye_adapter.py` 实现 `SourceAdapter`
2. 在 `ingest_wallets.get_sources()` append 一行 `BirdeyeAdapter()`
3. 完事 —— 不用动 orchestrator 也不用改 BQ schema

---

## Infra 层

### `infra/bigquery_schema.sql`

最初的 5 张表（wallets / raw_swaps / analyzed_swaps / sol_prices / wallet_classifications）的 `CREATE TABLE IF NOT EXISTS` 定义。每个表都用 `OPTIONS(description=...)` 写了用途说明。

### `infra/migrations/002_add_ingestion_tables.sql`

Phase 4 加的 4 张表 + `wallets` 表的 ALTER（加 status / filter_reason / promoted_from 字段，并 backfill 已有的 61 个钱包为 active+manual）。

**Migration 风格**：
- `CREATE TABLE IF NOT EXISTS` —— 重跑安全
- `ADD COLUMN IF NOT EXISTS` —— 同上
- backfill 用 `WHERE status IS NULL` —— 只动需要的行

⚠️ 没有 migration runner / version 表 —— **完全手动运行**：`bq query --use_legacy_sql=false < 002_add_ingestion_tables.sql`

### `infra/migrate_to_bigquery.py`

一次性脚本，把早期本地 `data/*.json` 文件灌进 BigQuery。WRITE_TRUNCATE 整个表，所以可以重跑。一个细节：analyzed swaps 在原 raw_swaps 没匹配 signature 时合成假 sig `MIGRATED_<wid>_<ts>` 来满足 NOT NULL 约束。

---

## 辅助脚本

### `scripts/smoke_test_credentials.py`
4 服务一键健康检查：Helius `getHealth` / Claude min ping / Dune query 1 / BQ `SELECT 1`。任一失败 exit 1。

### `scripts/test_dune_adapter.py`
绕过 BQ，单独验证 Dune adapter 能拿到候选。打印前 3 个。

### `scripts/show_scale.py`（5/13 加的）
一键打印当前项目规模快照：funnel、transactions、DEX 覆盖、tag 分布、SOL 价格历史、表大小、最近 ingestion runs。

### 根目录的"个人 / 历史"文件（gitignored）
- `Notes.py` / `Solana_config_info.py` / `test_pub.py` / `test_sub.py` —— 早期用 Solana RPC pubsub 的探索代码，没在 pipeline 路径上
- `check_swaps.py` —— 旧的本地 file-based reconciliation 工具，BQ 化后基本没用
- `llm.py` —— Claude API 给 wallet swap 历史写"市场做市商"二分类判断。**不在 pipeline 主路径，本地 only**，被 `.dockerignore` 排除

---

## 部署 & 运行机制

### Docker

[Dockerfile](Dockerfile) 三层：
1. `python:3.11-slim` base + 两个 ENV（`PYTHONDONTWRITEBYTECODE` + `PYTHONUNBUFFERED`）
2. `COPY requirements.txt + pip install` —— 单独一层 → 改代码不会触发 reinstall
3. `COPY . .` —— 最后 copy 代码（.dockerignore 排除 .env、llm.py、infra/、*.md 等）

CMD: `python main.py`

### Makefile 目标

| target | 做什么 |
|---|---|
| `test-local` | 直接 `python main.py` |
| `build-local` | 本地架构 docker build（Mac arm64）|
| `run-local` | docker run，把 ~/.config/gcloud/application_default_credentials.json mount 进去 |
| `build` | `--platform linux/amd64` 给 Cloud Run 用，tag = git short hash |
| `push` | build + push 两个 tag（`<hash>` + `latest`）到 Artifact Registry |
| `deploy` | push 后 `gcloud run jobs update --image=...` 指向新 image |
| `run` | `gcloud run jobs execute --wait` 手动触发一次 |
| `logs` | 列最近一次 execution 状态 |

### Cloud Run 设置
- Job 名: `smartwalletstracker-job`
- Region: `us-central1`
- Task timeout: **7200s**（5/13 从 3600s 提上来的）
- 触发: Cloud Scheduler，cron `0 9 * * *`（9am UTC 等于 4-5am ET，看夏令时）

---

## 当前状态 & 最近改动

### 数据规模（截至 2026-05-14）
```
wallets:             1,041 tracked
wallets_classified:  1,015
smart_candidates:    87
total_swaps:         160,656 (raw)
unique_signatures:   160,414 (99.85% unique)
raw_data_size:       2,666.9 MB
sol_prices:          1,400+ days
```

### 这几天的改动

**5/13** —— Cloud Run job 撞 3600s timeout：
- 根因：`filter_traders` 每次都把 1.5GB+ raw_swaps 全部 fetch 进 Python
- 修复：增量化 classification（`fetch_wallets_needing_classification` + `wallet_ids` 过滤参数）
- 一次性配置改动：task-timeout 提到 7200s

**5/13 加的工具**：
- `scripts/show_scale.py` — 项目规模 snapshot

**关键文件改动**（未 commit 状态）：
- `lib/bq.py` — 新增 `fetch_wallets_needing_classification`，给 `fetch_raw_swaps_all_wallets` / `fetch_analyzed_swaps_all_wallets` 加 `wallet_ids` 参数
- `filter_traders.py` — main 改成增量

### Helius 用量情况
- Free tier: 1M credits/月
- 当前 cycle 已用 250k+（27 天后 reset）
- 预测：再过 9 天可能撞上限
- 计划：先做 architecture 优化（async + N+1 elimination），然后 cycle 末决定是否升级 Developer tier

---

## 已知 gaps / 未完成的部分

### Pipeline 本身
- **`exchange_wallets` 表是空的** —— CEX 过滤逻辑写了但实际没生效，要手动 seed 50-200 个 CEX 地址
- **N+1 query 模式还存在**：`collect_traders_swaps` 里 `existing_signatures_for_wallet` 是 per-wallet 一次 BQ query。1000 钱包 = 1000 query
- **Stage 3 串行**：1000 钱包 sequential fetch，没用 asyncio
- **Helius DAS resolution 串行**：`analyze_wallets.resolve_token_symbols` 一个 mint 一个调，没批量
- **没有 alerting**：Job 失败靠你手动 `make logs` 才知道（Cloud Monitoring email alert 未配）

### 产品化（计划但未做）
- 没有 REST API（前几天写了 FastAPI 骨架但已删，决定先搞清楚现有 pipeline 再做）
- 没有前端
- 没有 mobile app
- QuerySmith（sibling project）独立 repo，未来可能合并进来

### 工程实践小瑕疵
- Migration 完全手动跑 —— 没有 migration runner / version tracking
- `data/` 目录被删过一次（gitignored 状态下 flatten 仓库时漏了）—— 备份还在 `SWT_BACKUP_20260425_203629/`
- DEPLOYMENT_NOTES.md / INTERVIEW_TALKING_POINTS.md 是 gitignored 的个人文档，公开 repo 上看不到

---

## 一图总结

```
                              [Cloud Scheduler 9am UTC]
                                          │ trigger
                                          ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Cloud Run Job: smartwalletstracker-job                              │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  main.py                                                       │  │
│  │   ↓                                                            │  │
│  │  Stage 1 ingest_wallets    [fail-soft]                         │  │
│  │   ↓ adapter pattern (Dune today, BirdEye/Arkham later)         │  │
│  │   ↓ writes: wallet_candidates, wallet_sources, wallets,        │  │
│  │   ↓         ingestion_runs                                     │  │
│  │   ↓                                                            │  │
│  │  Stage 2 fetch_sol_prices  [fail-hard]                         │  │
│  │   ↓ Binance free API, MERGE incremental                        │  │
│  │   ↓ writes: sol_prices                                         │  │
│  │   ↓                                                            │  │
│  │  Stage 3 collect_traders_swaps  [fail-hard]                    │  │
│  │   ↓ Helius /v0/addresses/.../transactions?type=SWAP           │  │
│  │   ↓ newest-first + sig hit-stop                                │  │
│  │   ↓ writes: raw_swaps, wallets.collection_status               │  │
│  │   ↓                                                            │  │
│  │  Stage 4 analyze_wallets  [fail-hard]                          │  │
│  │   ↓ anti-join on parser_version (189s→2s)                      │  │
│  │   ↓ Jupiter parser + token-transfer fallback                   │  │
│  │   ↓ Helius DAS getAsset for new token symbols                  │  │
│  │   ↓ writes: analyzed_swaps                                     │  │
│  │   ↓                                                            │  │
│  │  Stage 5 filter_traders  [fail-hard]                           │  │
│  │   ↓ NEW: incremental anti-join on analyzed_at                  │  │
│  │   ↓ 6 行为/PnL 标签 → smart_candidate 最终判定                 │  │
│  │   ↓ append-only snapshot                                       │  │
│  │   ↓ writes: wallet_classifications                             │  │
│  └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

---

**Last updated**: 2026-05-14 by an exhaustive code read of every source file.
